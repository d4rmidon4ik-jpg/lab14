[build-system]
requires = ["maturin>=1.0"]
build-backend = "maturin"

[project]
name = "tx-validator"
version = "0.1.0"
requires-python = ">=3.8"

[tool.maturin]
features = ["pyo3/extension-module"]
module-name = "tx_validator"